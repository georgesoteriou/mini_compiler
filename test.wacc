# common error exit statement

# Output:
# #empty#

# Exit:
# 255

# Program:

begin
  int g(int a, int b, int c) is
    string p = call f();
    print "g ";
    println p;
    return 1
  end

  string f() is
    string s = "hello";
    return s
  end

  pair(int, string) ab = null;

  pair(int, string) abc = newpair(5, "hello");

  int[] arr = [1,2,4,5];

  arr = [1,2,3,4,5,6];

  fst abc = 4;
  snd abc = "World";

  int kk = fst abc;


  int a = 3;
  int x = a;

  string s = call f();

  if x == a then
    println s;
    println "YAY"
  else
    print 2;
    println 2 + 2
  fi;

  while a > 0 do
    if a == 1 then
        println "a is 1"
    else
        println "a is not 1"
    fi;

    a = a - 1;
    println a
  done;

  char c = 'b';
  println c;
  skip;

  int y = call g(1, 2, 3);
  println y;

  exit -1
end